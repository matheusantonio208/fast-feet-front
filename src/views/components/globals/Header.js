import React, { useState } from 'react';

import { Container } from '#styles/components/header-style.js';

export default function ComponentName() {
  const [stateName, setStateFunction] = useState([]);

  return (
    <Container>
      <div />
    </Container>
   );
}
